# Core dependencies (CLI)
pandas>=1.5.0
PyYAML>=6.0
pydantic>=2.0.0
pandera>=0.18.0

# Full install dependencies (dashboard)
# Install with: pip install uqcme[full]
# streamlit>=1.28.0
# plotly>=5.15.0
# openpyxl>=3.0.0
